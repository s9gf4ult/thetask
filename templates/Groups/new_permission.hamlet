<h1> add permission to
  <a href=@{GroupsR $ MPiece gid $ MGroupStd MEmpty}>#{groupName group}

<ul>
  $forall fail <- fails
    <li> #{fail}

<form method=post action=@{GroupPermissionsR $ CPiece CEmpty} enctype=#{enctype}>
  ^{widget}
  <button>Grant
